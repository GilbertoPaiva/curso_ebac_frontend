const multiplicar = (a: number, b: number): number => a * b;

const saudacao = (nome: string): string => `Olá ${nome}`;
console.log('Multiplicação (3 x 4):', multiplicar(3, 4));
console.log('Saudação:', saudacao('EBAC'));